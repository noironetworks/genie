formatter
{
    domain[dvorkin]
    {
        feature[mike]
        {
            task[eat;
                 target=generic;
                 category=mclass;
                 relative-path=here/there/some/place;
                 file-type=c:header;
                 file-prefix=Prefix;
                 file-suffix=Suffix;
                 formatter=genie.test.format.TestOne;
                 ]

            task[sleep;
                 target=generic;
                 category=mclass;
                 relative-path=here/there/some/place;
                 file-type=c:header;
                 file-prefix=Prefix;
                 file-suffix=Suffix;
                 formatter=genie.test.format.TestOne;
                 ]
        }
        feature[marie]
        {
            task[poop;
                 target=generic;
                 category=mclass;
                 relative-path=here/there/some/place;
                 file-type=c:header;
                 file-prefix=Prefix;
                 file-suffix=Suffix;
                 formatter=genie.test.format.TestOne;
                 ]

            task[love;
                 target=generic;
                 category=mclass;
                 relative-path=here/there/some/place;
                 file-type=c:header;
                 file-prefix=Prefix;
                 file-suffix=Suffix;
                 formatter=genie.test.format.TestOne;
                 ]
        }
    }
}